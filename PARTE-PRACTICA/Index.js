const studentGrade = [{ Nota: A }];

const studentGrades = [100, 50, 65, 70, 85, 96, 45];
const result = computeGrades(studentGrades);
console.log(result);

console = {
	parsed: [A, F, D, C, B, A, F],
	passed: 5,
	lose: 2,
};
